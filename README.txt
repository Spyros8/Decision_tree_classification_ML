#Decision Tree learning algorithm


-Run main.py to generate data shown in the report


-For the user to perform training, run decision_tree_learning, prune. Then run classift_array to generated classification metrics.
main for clean report data


-To train and validate the tree once, run the single_validation function. The tree then gets automatically pruned
